// type could be bottle

export type PoiType = {
    name: string;
    location: string | null;
    latitude: number;
    longitude: number;
    // fillBottle: boolean;
    // flagCount: number;
    // active: boolean;
    // lastUserIdUpdated: string;
};
